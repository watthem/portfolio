routes:

collections:
  /:
    permalink: /{slug}/
    template: index
  /work/:
    permalink: /work/{slug}/
    filter: tag:work
    order: asc

taxonomies:
  tag: /portfolio/{slug}/
  author: /author/{slug}/
