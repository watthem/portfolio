routes:

collections:
  /:
    permalink: /{slug}/
    template: index
  /portfolio/:
    permalink: /writing/{slug}/
    filter: primary_tag:writing
    order: asc

taxonomies:
  tag: /portfolio/{slug}/
  author: /author/{slug}/
