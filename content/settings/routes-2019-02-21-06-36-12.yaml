routes:

collections:
  /work/:
    permalink: /work/{slug}/
    filter: tag:work
    order: desc
  /:
    permalink: /{slug}/
    template: index
    filter: tag:-work

taxonomies:
  tag: /portfolio/{slug}/
  author: /author/{slug}/
