routes:

collections:
  /:
    permalink: /{slug}/
    template: index
  /portfolio/:
    permalink: /portfolio/{slug}/
    filter: primary_tag:portfolio
    order: asc

taxonomies:
  tag: /portfolio/{slug}/
  author: /author/{slug}/
