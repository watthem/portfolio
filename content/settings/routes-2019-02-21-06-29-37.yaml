routes:

collections:
  /work/:
    permalink: /work/{slug}/
    filter: tag:work
    order: desc
  /:
    permalink: /{slug}/
    template: index

taxonomies:
  tag: /portfolio/{slug}/
  author: /author/{slug}/
